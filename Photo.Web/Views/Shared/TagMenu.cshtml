@model IEnumerable<Photo.Web.Models.Tag>

<h2>热门分类</h2>
<ul>

    @if (Model != null)
    {
        foreach (var item in Model)
        {
            <li>
                <a href="/tag/@item.ID" target="_blank" >@item.Name</a>
            </li>
        }
    }
</ul>